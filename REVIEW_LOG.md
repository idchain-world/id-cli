# Review Log

## Item 52 — `getProvider(chainId)` | Status: PASS | 2026-03-23 | Reviewed `getProvider` in `src/provider.ts:18-21`. The function is a thin wrapper that retrieves chain config via `getChainConfig(chainId)` and returns a new `ethers.JsonRpcProvider` using the configured RPC URL. The chain config lookup in `config.ts` correctly supports per-chain env var overrides (`RPC_URL_BASE`, etc.), a global `RPC_URL` override, and built-in defaults, with proper error handling for unsupported chain IDs. The function is clean, minimal, and correct — no bugs or issues found.

## Item 148 — URL protocol validation (https/http/ipfs) | Status: PASS | 2026-03-23 | Reviewed `validateUrl()` at `src/commands/agent-endpoints.ts:18-26`. The function checks that a URL string starts with `https://`, `http://`, or `ipfs://` and throws a `CliError` with `INPUT_ERROR` exit code if none match, including the offending value and flag name in the error message. It is called at line 50 inside the `setAgentEndpointsCommand` action for each protocol flag (`--mcp`, `--a2a`, `--oasf`, `--web`). The validation is straightforward prefix matching via `startsWith`, which is appropriate for this use case — the CLI only needs to reject obviously wrong inputs (missing protocol, `ftp://`, etc.), while the endpoint itself is stored as an opaque text record and validated by consuming clients. The function correctly returns the original URL string for chaining. The `--clear` path correctly bypasses URL validation since it writes empty strings. No bugs or issues found.

## Item 217 — assert(condition, message) | Status: PASS | 2026-03-23 | Reviewed the custom `assert()` helper at `test/test.ts:92-94`. This is a minimal one-liner that throws `new Error(`Assertion failed: ${message}`)` when the condition is falsy, used within a hand-rolled integration test framework that runs CLI commands against a local Anvil fork. It is called 5 times across the test file for cases where `assertIncludes` (string containment) isn't appropriate: extracting a regex match result (line 210, 424), verifying one of multiple valid output patterns (line 389-392), and checking non-empty error output (line 471). The thrown error is caught by the `test()` and `testOptional()` wrappers which track pass/fail/skip counts. The function is correct, minimal, and fit for purpose — it provides clear failure messages with the assertion context, and its usage is consistent with the lightweight test harness design. No bugs or issues.

## Item 66 — `ENS_NAME_WRAPPER_ABI` | Status: PASS | 2026-03-24 | Reviewed the ABI at `src/abi.ts:58-61`. It exposes two functions: `ownerOf(uint256 id) view returns (address)` and `setResolver(bytes32 node, address resolver)`. These are used in `src/commands/link-ens.ts` in two contexts: (1) `checkEnsOwnership()` at line 83 calls `wrapper.ownerOf(ensNameNode)` to resolve the true owner of a wrapped name when the ENS registry reports the NameWrapper contract as the owner — this correctly handles the ENS NameWrapper indirection; (2) at line 276, `wrapper.setResolver()` is called to set the resolver for wrapped names, with a separate `registry.setResolver()` path for unwrapped names. The ABI is minimal and correct — it includes exactly the two functions the CLI needs. The `ownerOf` takes `uint256` (matching ERC-1155 token IDs used by NameWrapper) and the `setResolver` takes `bytes32 node`, both matching the actual NameWrapper contract signatures. No issues found.

## Item 166 — ENS commit/reveal 60-second delay | Status: PASS | 2026-03-24 | Reviewed the commit/reveal wait logic at `src/commands/register-ens.ts:120-128`. After submitting the commitment transaction (line 115-117), the code waits 60 seconds via a countdown loop: `for (let i = 60; i > 0; i -= 10)` with `setTimeout(r, Math.min(i, 10) * 1000)`. The loop correctly sleeps 10 seconds per iteration for 6 iterations (i=60,50,40,30,20,10), totaling exactly 60 seconds. The `Math.min(i, 10)` guard is harmless since `i` is always >= 10 when the loop body runs. The 60-second delay matches the ENS ETHRegistrarController's `minCommitmentAge` anti-front-running requirement. Status logs show a countdown every 10s, and the commit secret is generated via `ethers.randomBytes(32)` (cryptographically secure). The same secret and parameters are reused for the register call at line 132-134, ensuring the reveal matches the commit. Correct implementation of the ENS two-step pattern.

## Item 230 — Ownership check before every write transaction | Status: REVIEW | 2026-03-23 | Reviewed `verifyOwnership()` at `src/utils.ts:58-71`. The function itself is correct: it queries `registry.owner(node)`, rejects zero-address (unregistered name), and does a case-insensitive comparison against the caller's wallet. It is properly called in 5 out of 7 write-command files: `set-record.ts`, `records.ts`, `subname.ts`, `transfer.ts`, and `agent-endpoints.ts`. However, two write paths in `agent.ts` skip it: `registerAgentCommand` (line 143) calls `nameRegistry.setText()` to set an ENSIP-25 link record without verifying the caller owns the name, and `linkAgentCommand` (line 207) likewise calls `registry.setText()` without ownership verification. While the on-chain registry's `setText` modifier will revert if the caller isn't authorized, the missing client-side check means users get an unhelpful EVM revert error instead of a clear CLI error message like "You don't own <domain>". This is a UX gap rather than a security bug (the contract enforces access control), but it's inconsistent with every other write command in the CLI and should be addressed for parity.

## Item 180 — Package name: @xid/cli | Status: REVIEW | 2026-03-24 | Reviewed `package.json` (29 lines). The package is scoped as `@xid/cli` v0.2.8 with `"type": "module"` for ESM. The `bin` field correctly maps the `id-cli` command to `./dist/index.js`, and the dist file exists with a proper `#!/usr/bin/env node` shebang and executable permissions. The `name` field in Commander (line 22 of `src/index.ts`) matches the bin name (`id-cli`), and the description is consistent between package.json and the Commander program. The `version` in package.json (`0.2.8`) matches the hardcoded version in `src/index.ts:24`. Dependencies are minimal and appropriate: `ethers`, `commander`, `chalk`, `dotenv` for production; `typescript`, `tsx`, `vitest`, `@types/node` for dev. However, several standard package.json fields are missing: no `main` or `exports` (fine for a pure CLI, but would help tooling), no `files` array (risks publishing unnecessary files like `test/`, `REVIEW_LOG.md`, `.claude/`), no `license` field, no `repository`/`homepage`/`author`/`keywords` metadata, and no `engines` field to declare minimum Node version (the ESM + top-level await usage requires Node 14.8+). The missing `files` field is the most impactful — without it, `npm publish` would include everything not in `.npmignore`, potentially shipping test fixtures, agent configs, and review artifacts. Marking REVIEW for the missing `files` field.

## Item 197 — Strict mode: enabled [tsconfig.json] | Status: PASS | 2026-03-24 | Reviewed `tsconfig.json` (16 lines). The configuration enables `"strict": true` which activates all strict type-checking flags (`strictNullChecks`, `strictFunctionTypes`, `strictBindCallApply`, `strictPropertyInitialization`, `noImplicitAny`, `noImplicitThis`, `alwaysStrict`). The project compiles cleanly with zero errors under strict mode. Target is `ES2022` (appropriate for Node 16+ with top-level await support), module system is `Node16` (correct for ESM with `.js` import extensions used throughout), and `moduleResolution: "Node16"` matches. Other settings are sensible: `declaration: true` generates `.d.ts` files, `sourceMap: true` aids debugging, `resolveJsonModule: true` allows JSON imports, and `skipLibCheck: true` speeds up compilation. The `include` is scoped to `src/**/*` so test files don't interfere with the build. There are ~33 instances of `any` across the source — most are in `catch (err: any)` clauses (14 occurrences, idiomatic for TypeScript error handling), `outputSuccess(data: any)` (intentionally flexible for JSON serialization), and API response mappings (`d: any` for indexer JSON). Three uses in `schema.ts` access Commander internals via `(cmd as any)._args` which is unavoidable without typed Commander extensions. None of these undermine the value of strict mode — the critical paths (name resolution, ABI encoding, transaction building) are fully typed. No issues found.

## Item 228 — Private key regex validation [provider.ts:31] | Status: PASS | 2026-03-24 | Reviewed the private key validation in `getWallet()` at `src/provider.ts:23-49`. The regex `/^(0x)?[0-9a-fA-F]{64}$/` correctly validates 256-bit Ethereum private keys: it anchors at both ends (`^` and `$`), optionally accepts the `0x` prefix, and requires exactly 64 hex characters (case-insensitive). The validation flow is well-structured: (1) check `PRIVATE_KEY` is set, throwing `AUTH_ERROR` with a helpful message listing read-only commands that don't need it; (2) regex-validate the format, throwing `INPUT_ERROR`; (3) warn if loaded from `.env` file (via `isKeyFromDotenv()` helper); (4) construct the wallet inside a try/catch that catches any remaining ethers parsing failures. Critically, none of the error messages include the actual `pk` value — this was explicitly addressed in commit `a7c3f5c` ("Sanitize PRIVATE_KEY error to prevent key leakage"). The `isKeyFromDotenv()` helper at lines 7-16 is a reasonable heuristic (checks if `.env` file exists and contains `PRIVATE_KEY=`), and its failure mode is safe (returns `false` on any error, suppressing the warning rather than crashing). The only minor note is that `console.warn` at line 38 bypasses the CLI's `humanLog`/`statusLog` output system, but this is intentional — the warning should always appear regardless of `--output json` mode. No bugs or security issues found.

## Item 59 — `resolveChain(chain)` | Status: REVIEW | 2026-03-24 | Reviewed `resolveChain()` at `src/config.ts:109-120`. The function resolves a user-provided chain string (name, shortName, or numeric ID) to a chainId integer. It is called from 8 locations across the CLI (register, agent, explore, mint, register-ens, and utils). The core lookup at lines 111-113 uses `Object.values(CHAIN_CONFIGS).find()` to match on `shortName` or `name` (both case-insensitive), which correctly handles inputs like `"base"`, `"Base"`, `"eth"`, `"Ethereum"`, `"sep"`, `"Sepolia"`, etc. Two issues: (1) **Dead code on line 116**: the check `if (chain.toLowerCase() === "sepolia") return 11155111` is unreachable because `CHAIN_CONFIGS[11155111].name` is `"Sepolia"`, which is already matched by `c.name.toLowerCase() === chain.toLowerCase()` in the `find()` on line 112. This alias was likely added before the `name` field was included in the lookup, and should be removed. (2) **Lax integer parsing on line 117**: `parseInt(chain)` accepts trailing garbage (e.g., `parseInt("42161abc")` returns `42161`), meaning `resolveChain("42161abc")` would silently resolve to Arbitrum instead of throwing an error. While unlikely in practice since inputs come from CLI `--chain` flags, `Number(chain)` or a regex check would be stricter. Neither issue causes bugs in normal usage, but the dead code is misleading and the parseInt laxness is inconsistent with the strict validation elsewhere in the CLI. Marking REVIEW.

## Item 163 — Permit fallback to approve() on failure [register.ts:106-111] | Status: PASS | 2026-03-24 | Reviewed the EIP-2612 permit-with-fallback flow at `src/commands/register.ts:101-111`. The `permit` variable is initialized with zero values on line 102 (`deadline: 0n, v: 0, r: ethers.ZeroHash, s: ethers.ZeroHash`) before attempting `signUsdcPermit()`. If the permit signing succeeds (line 105), the returned `{deadline, v, r, s}` overrides the defaults and is passed to the registrar contract. If permit fails (e.g., USDC on Sepolia may not support EIP-2612, or the RPC rejects `eth_signTypedData_v4`), the catch block at line 106 logs a truncated error message (`permitErr.message?.slice(0, 60)` — good for avoiding log noise), then falls back to a standard `usdc.approve()` followed by `approveTx.wait()`. The zero permit values are then passed to `register()`/`registerWithParent()` at lines 120/126, which the contract interprets as "no permit, use existing allowance" — standard EIP-2612 behavior where `deadline=0` with zero v/r/s signals the contract to skip `permit()` and rely on the pre-approved allowance. The `signUsdcPermit()` function at `utils.ts:214-247` correctly implements EIP-2612 typed data signing with the standard Permit struct, 1-hour deadline, and proper domain separator including `chainId` and `verifyingContract`. The only minor observation is that `permitErr.message?.slice(0, 60)` uses optional chaining correctly for the case where `message` might be undefined. No bugs or issues found.

## Item 17 — `explore` command [src/commands/explore.ts] | Status: REVIEW | 2026-03-24 | Reviewed the `explore` command at `src/commands/explore.ts` (97 lines). The command lists registered agent names from the indexer with support for chain filtering, search, pagination, and owner lookup. The over-fetching strategy at line 31 (`requestLimit = limit + 50`) accounts for entries removed by client-side filtering (hidden names, null entries, suffix validation) — a reasonable heuristic, though it can under-deliver if many entries are filtered. The `HIDDEN_NAMES` set and `INDEXER_CHAINS` map are correct for the current 5 chains. Two issues found: (1) **Missing offset validation**: `opts.offset` is passed raw as a string to the URL on lines 34 and 38 without calling `parseNonNegativeInt()`. By contrast, `subname.ts:73` validates its offset via `parseNonNegativeInt(opts.offset, "--offset")`. A user passing `--offset abc` would silently send `offset=abc` to the indexer API, which might return unexpected results or default to 0. This is inconsistent with the limit validation on the same command (line 31 and 69 both parse limit). (2) **Double parsing of limit**: `opts.limit` is parsed via `parseNonNegativeInt` on both line 31 (to compute `requestLimit`) and line 69 (to compute display limit). This is wasteful but harmless — storing the parsed value in a variable on first use would be cleaner. Neither issue causes user-visible bugs in normal usage, but the missing offset validation is an input validation gap that should be fixed for consistency. Marking REVIEW.

## Item 208 — records.test.ts (Record management logic) | Status: REVIEW | 2026-03-24 | Reviewed `test/unit/commands/records.test.ts` (127 lines, 12 tests). The tests cover four command areas: `set-text` dry-run proposal building (1 test), `set-addr` coin type parsing and ABI selection (5 tests), `set-contenthash` dry-run proposal (1 test), and `records --select` field filtering (4 tests). All tests are pure unit tests that exercise logic without mocking contracts or network calls — a sound approach. The `--select` field parsing logic at lines 102-105 correctly mirrors the production code at `records.ts:35-37`. Two issues noted: (1) **Naming inconsistency**: the test at line 39 expects `parseNonNegativeInt("0", "--coin-type")` to return 0, which it does because the function at `utils.ts:50-56` checks `n < 0` — but the function name "parseNonNegativeInt" is misleading since 0 is not a positive integer. The error message says "non-negative integer" which is correct, but the function name should match. (2) **Missing coverage**: the `set-record.ts` bulk record command (143 lines of pair-parsing, validation, and byte-encoding logic) has zero test coverage in this file despite being the most complex record-related command. The key=value splitting, coin type validation, ETH address encoding via `zeroPadValue`, and data record byte conversion are all untested. Marking REVIEW for the naming inconsistency and the coverage gap on `set-record`.

## Item 78 — ID_AGENT_REGISTRAR (5 addresses) | Status: PASS | 2026-03-24 | Reviewed all 5 `ID_AGENT_REGISTRAR` addresses in `config.ts:26,39,52,65,78` and their usage in `register.ts`. Each chain has a unique, valid 42-char Ethereum address (validated by `config.test.ts:21` regex). The registrar address is used in 4 places within `register.ts`: (1) contract instantiation at line 27 with `REGISTRAR_ABI`, (2) dry-run proposal `contractAddress` at line 88, (3) USDC EIP-2612 permit spender at line 105 via `signUsdcPermit()`, and (4) USDC `approve()` fallback spender at line 108. All four usages correctly target the registrar as the spender — the contract pulls USDC from the user during registration. The `REGISTRAR_ABI` at `abi.ts:1-7` defines 5 functions (`register`, `registerWithParent`, `price`, `nextLabel`, `nextAgentId`); the first four are actively called, `nextAgentId` is defined but unused in the CLI (harmless — available for future use). No hardcoded address duplicates exist outside `config.ts` — all references go through `config.ID_AGENT_REGISTRAR`. Address format, uniqueness, and usage consistency are all correct. No issues found.

## Item 49 — `humanLog(msg)` | Status: REVIEW | 2026-03-24 | Reviewed `humanLog()` at `src/output.ts:61-65`. The function guards output behind `!isJsonOutput()` and calls `console.log(msg)` — correctly suppressing human-readable messages in JSON mode so that stdout remains clean for machine-parseable JSON envelopes via `outputSuccess()`. It is extensively used across 15 command files (~90+ call sites) for all interactive output: domain info, tx hashes, record listings, status confirmations, etc. The output format detection in `getOutputFormat()` (lines 8-18) is sound: explicit `--output json/human` flag takes priority, otherwise auto-detects via `process.stdout.isTTY`. The format is cached in `_format` after first call, avoiding repeated argv scanning. **Issue: JSDoc comment is incorrect.** The comment on line 60 says "Log to stderr in human mode" but the function uses `console.log(msg)` which writes to **stdout**, not stderr. The sibling function `statusLog` at line 67 has a similar comment inaccuracy — "Log to stderr always" but it only uses `process.stderr.write` in JSON mode; in human mode it also uses `console.log` (stdout). The actual behavior of both functions is correct for the CLI's dual-output design (human output on stdout, JSON envelopes on stdout, progress on stderr in JSON mode), but the misleading comments could confuse contributors into thinking humanLog writes to stderr. Marking REVIEW for the documentation mismatch.

## Item 6 — `set-addr` command | Status: REVIEW | 2026-03-25 | Reviewed the `set-addr` command at `src/commands/records.ts:123-185`. The command sets an address record on a name, supporting both ETH (coinType 60, the default) and multi-coin addresses via the `--coin-type` flag. It correctly uses two overloaded ABI signatures: `setAddr(bytes32,address)` for ETH and `setAddr(bytes32,uint256,bytes)` for other coin types, disambiguated via `registry.getFunction(...)`. Ownership verification via `verifyOwnership()` is present (line 164), dry-run is supported with correct ABI proposals for both branches, and `parseNonNegativeInt` validates `--coin-type`. **Issue: Missing address validation for coinType 60.** When coinType is 60 (ETH), the `<address>` positional argument is passed directly to the contract at line 168 without calling `validateAddress()`. Every other write command that accepts an Ethereum address uses `validateAddress()`: `register.ts:69` (`--address`), `subname.ts:23` (`--owner`), `transfer.ts:18` (`--to`), `set-reverse.ts:76` (`--addr`). Without it, a malformed ETH address (e.g., missing chars, bad checksum) produces a generic ethers.js error instead of a clear `CliError` with `INPUT_ERROR` exit code and helpful message like `Invalid address for <address>: "..."`. For non-60 coin types, `ethers.getBytes(address)` at line 175 provides basic hex validation, but its error message is also generic. The fix is straightforward: add `address = validateAddress(address, "<address>")` at the top of the coinType === 60 branch (both dry-run and execution paths). Marking REVIEW for this input validation gap.

## Item 74 — Arbitrum chain config (42161) | Status: PASS | 2026-03-25 | Reviewed the Arbitrum chain configuration at `config.ts:55-67`. All fields are correct: chainId `42161` is Arbitrum One, shortName `"arb"` matches the standard abbreviation, suffix `".arb.xid.eth"` is unique across the 5 chains, RPC `"https://arb1.arbitrum.io/rpc"` is the official Arbitrum endpoint, and explorer `"https://arbiscan.io"` is the correct block explorer. The PARENT_NODE is a unique 32-byte namehash. Contract addresses are all valid 40-hex-char addresses: `ID_REGISTRY` and `ID_AGENT_REGISTRAR` are unique to Arbitrum; `IDENTITY_REGISTRY_8004` shares the `0x8004...` vanity address with all other mainnets (deterministic deployment); `MOCK_USDC` shares `0xeaF7aC99F4667804c40D171c82e4CB2C93DeF299` with Optimism (likely a deterministic CREATE2 deployment of the project's fee token). Integration across the codebase is consistent: `explore.ts:14` maps 42161 to `"arbitrum"` for the indexer, `link-ens.ts:46` correctly routes Arbitrum through the unified resolver on mainnet (L2 pattern), `set-reverse.ts:19` uses the standard L2 reverse registrar `0x0000...D8e504...` shared with Base and Optimism, and the reverse namespace `8000a4b1.reverse` correctly encodes chainId 42161 (hex `0xa4b1` zero-padded to 8 chars). The RPC env override key is `RPC_URL_ARB` at `config.ts:91`. Tests cover resolution via shortName, full name, and numeric ID. No issues found.

## Item 210 — test/unit/commands/subname.test.ts | Status: PASS | 2026-03-25 | Reviewed `test/unit/commands/subname.test.ts` (99 lines, 10 tests across 2 describe blocks). The `create-subname` tests (7 tests) cover: sublabel validation via `validateLabel()` for valid inputs (`"neo"`, `"test-sub"`, `"a"`) and invalid ones (`"NEO"`, `""`, `"has space"`), matching the regex `/^[a-z0-9]+(-[a-z0-9]+)*$/` at `utils.ts:31`; full domain construction `sublabel.parent.domain` mirroring `subname.ts:25`; owner address validation via `validateAddress()` for both valid and invalid inputs; dry-run proposal structure at lines 49-57 that exactly mirrors the production `proposeTx` call at `subname.ts:28-36` (same ABI signature `setSubnodeOwner(bytes32,string,address)`, same argLabels `["parentNode","label","newOwner"]`, same args order); and a node computation invariant check verifying `makeNode(parent.node, "neo")` equals `resolveName("neo.agent-0", "base").node` — confirming the keccak256 parent-child hash chain is consistent. The `list-subnames` tests (3 tests) cover pagination parsing and indexer URL construction matching `subname.ts:75`. Tests correctly use the synchronous `resolveName` (the core that `resolveNameAsync` wraps) for unit testability. All assertions match production code paths. No issues found.

## Item 3 — `info` command | Status: REVIEW | 2026-03-25 | Reviewed the `info` command at `src/commands/info.ts` (89 lines). The command is read-only and correctly uses `getProvider()` (not `getWallet()`) since no signing is needed. On-chain queries for `owner`, `addr(bytes32)`, and `isLocked` are parallelized via `Promise.all` (lines 24-28), with safe `.catch()` defaults for addr (ZeroAddress) and isLocked (false). The overloaded `addr` function is correctly disambiguated via `getFunction("addr(bytes32)").staticCall()`. Unregistered names (owner === ZeroAddress) are handled at lines 30-34 with both human and JSON output. The `--brief` flag correctly skips the indexer fetch. **Issue: In non-brief mode, the indexer fetch at `/api/domains/{node}/records` (line 54) returns the same response as the `records` command, which includes `textRecords`, `addressRecords`, `dataRecords`, and `contenthash`. However, `info` only extracts `textRecords` and `addressRecords` (lines 57-58), silently dropping `dataRecords` and `contenthash` from both the human output and the JSON envelope.** The `records` command at `records.ts:56-71` shows all four record types from the same endpoint. While `info` is an overview command and showing fewer record types is a reasonable design choice, the JSON output at lines 77-84 omits data that was already fetched, which could be surprising for agents parsing the structured output. A minor secondary note: if the ETH address resolved on-chain (line 26) also appears in the indexer's `addressRecords` as coinType 60, it could display twice in human output (line 41 and line 68). Marking REVIEW for the incomplete record extraction.
